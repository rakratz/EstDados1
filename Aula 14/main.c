#include <stdio.h>
#include "lista.h"

int main()
{
    Lista L;
    inicializaLista(&L);

    // Verificar se existe a lista e está vazia
    printf("A lista existe e esta vazia? \n");
    if (estaVazia){
        printf("Sim e ");
    }
    imprimeLista(&L);
    return 0;
}